# 多域名部署方案（已准备好，待 SSH 恢复后一键执行）

## 当前状态

- ✅ **个人网站正常运行**：http://106.54.44.45 （首页、API 全部可用）
- ⚠️ **SSH 管理通道暂时不可用**：sshd 过载（之前频繁连接导致），banner 无响应
- ✅ **多域名部署文件已全部准备好**：`/tmp/caddy-multidomain.zip`

## 恢复 SSH 的方法（选一个）

SSH 不可用只影响管理操作，不影响网站运行。恢复方法：

**方法 1：腾讯云控制台重启实例（最简单）**
1. 登录腾讯云控制台 → 云服务器 CVM
2. 找到这台服务器（公网 IP 106.54.44.45）
3. 点"重启"（网站会断 1-2 分钟，重启后自动恢复）
4. 重启后 SSH 和网站都会恢复

**方法 2：用控制台的 VNC 登录**
1. 腾讯云控制台 → 点"登录"或"VNC 连接"
2. 在 VNC 窗口里执行：`Restart-Service sshd`（PowerShell）
3. 不用重启整机

**方法 3：等它自己恢复**
Windows OpenSSH 通常会在一段时间后自恢复。可以每隔 10 分钟试一次：
```bash
echo "" | nc -w 5 106.54.44.45 22 | head -1
# 看到 "SSH-2.0-..." 就说明恢复了
```

## SSH 恢复后，执行多域名部署

### 第 1 步：买好域名并解析

在域名服务商（阿里云/腾讯云/Cloudflare）把每个域名的 **A 记录**都解析到 `106.54.44.45`。
例如买了 `martin.com` 和 `mindtree.com`：
- `martin.com` → A → 106.54.44.45
- `www.martin.com` → A → 106.54.44.45
- `mindtree.com` → A → 106.54.44.45

解析一般 1-10 分钟生效。

### 第 2 步：上传部署文件

```bash
# 用 paramiko 工具上传（SSH 恢复后）
python3 /tmp/rssh.py --file /tmp/caddy-multidomain.zip C:/caddy-multidomain.zip

# 解压
python3 /tmp/rssh.py "powershell -Command Expand-Archive C:\caddy-multidomain.zip -DestinationPath C:\portfolio -Force"
```

### 第 3 步：编辑 Caddyfile，填入你的真实域名

```bash
# 在服务器上编辑（或者先在本地改好再上传）
# 把 C:\portfolio\Caddyfile 里的 example.com 改成你买的域名
python3 /tmp/rssh.py "notepad C:\portfolio\Caddyfile"
# 或在本地编辑 /tmp/caddy-deploy/Caddyfile 后重新上传
```

Caddyfile 里的关键部分（改这两行的域名）：
```
martin.example.com, bcy.asia, www.bcy.asia {
    reverse_proxy 127.0.0.1:8080
    ...
}
```

### 第 4 步：一键部署

```bash
python3 /tmp/rssh.py "cmd /c C:\portfolio\setup-multidomain.bat"
```

这个脚本会自动：
1. 让 gateway 改听 8080（让出 80 给 Caddy）
2. 安装 Caddy 并注册为 Windows 服务（开机自启）
3. Caddy 监听 80，按域名分发到不同后端
4. 重启所有服务

### 第 5 步：验证

```bash
# 用域名访问（DNS 生效后）
curl http://你的域名1/health
curl http://你的域名2/    # 如果配了第二个站点
```

## 架构说明

部署后的拓扑：
```
访客 → Caddy (:80, 按域名分发) ─┬─ 域名1 → gateway.js (:8080) → NestJS(:3000) 个人网站
                                ├─ 域名2 → MindTree (:18789)
                                └─ 兜底 → 个人网站
```

- **Caddy** 作为前置网关，占 80 端口，按 `Host` 头分发
- **gateway.js** 退到 8080，只服务个人网站
- **NestJS** 继续在 3000，不变
- 以后要加新站点，只需在 Caddyfile 加一个 block（3 行），reload 即可

## 回滚（万一出问题）

```bash
python3 /tmp/rssh.py "cmd /c C:\portfolio\rollback.bat"
```
会卸载 Caddy、恢复 gateway 直听 80（回到现在的单域名状态）。

## 以后上 HTTPS（域名确定后）

只需在 Caddyfile 里把域名单独写在站点块（不要用 `:80` 兜底），Caddy 会自动：
1. 监听 443
2. 向 Let's Encrypt 申请免费证书
3. 把 80 自动 301 跳转到 443

零成本、零配置、自动续期。
